import { ForgotPasswordPanel } from "@/components/auth/ForgotPasswordPanel";

export const metadata = {
  title: "Forgot password — Stages",
};

export default function ForgotPasswordPage() {
  return <ForgotPasswordPanel />;
}
