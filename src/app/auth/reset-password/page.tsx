import { ResetPasswordPanel } from "@/components/auth/ResetPasswordPanel";

export const metadata = {
  title: "Reset password — Stages",
};

export default function ResetPasswordPage() {
  return <ResetPasswordPanel />;
}
